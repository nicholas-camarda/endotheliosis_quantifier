## ADDED Requirements

### Requirement: Bootstrap documentation SHALL reference collaborator artifact distribution packaging

Documentation surfaced alongside **`eq labelstudio start`** onboarding MUST reference the **`collaborator-distribution-packaging`** capability for obtaining companion weights, mask releases, and runtime roots outside Git defaults.

#### Scenario: Collaborator reads README bootstrap section

- **WHEN** a user reads repository guidance for Label Studio bootstrap
- **THEN** that guidance MUST link or summarize non-Git artifact acquisition consistent with **`collaborator-distribution-packaging`**
